<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.programmierenlernenhq.reha_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_menue"
            android:label="@string/title_activity_menue"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity_tp_erstellen"
            android:label="@string/title_activity_tp_erstellen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity_pat_suche"
            android:label="@string/title_activity_pat_suche"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>